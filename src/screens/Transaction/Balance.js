import React from 'react';
import { Text, StyleSheet, View } from 'react-native'

const balance = props => {
   return (
      <View style={{flex:1}}>
         <Text>Balance Tab</Text>
      </View>
   );
};

export default balance;
