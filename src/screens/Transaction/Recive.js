import React from 'react';
import { Text, StyleSheet, View } from 'react-native'

const recive = props => {
   return (
      <View style={{flex:1}}>
         <Text>Recive Tab</Text>
      </View>
   );
};

export default recive;
